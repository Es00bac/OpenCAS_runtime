"""Behavioral eval suites for OpenCAS.

These are outcome-focused evals, not unit tests. They measure whether
subsystems produce correct real-world behavior (retrieval recall, approval
accuracy, spark generation, lane completion) rather than just whether code
runs without raising.

Run via: python scripts/run_behavioral_evals.py
"""
